# Raw ingredient chroma renders (source for scripts/key-ingredients.mjs;
# keyed outputs live in public/assets/ingredients/ and ARE committed)
raw-ingredients/

# Original full-res dish/cover renders (source for scripts/optimize-images.mjs;
# optimized outputs live in public/assets/ and ARE committed)
raw-assets/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Raw category chroma renders (keyed outputs in public/assets/categories/ ARE committed)
raw-categories/

# Downloaded video renditions (regenerate with `npm run fetch:videos`).
# Large (~120MB across resolutions) — kept out of git; app falls back to posters.
public/assets/video/
